# Server-side only. Use separate credentials in each environment.
GOOGLE_GENERATIVE_AI_API_KEY=
POSTGRES_URL=
# Configure a dedicated database role with SELECT on public.access_events only.
ACCESS_AUTH_USERNAME=analyst
# Generate at least 32 random ASCII characters, e.g. openssl rand -hex 32.
ACCESS_AUTH_PASSWORD=
